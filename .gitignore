# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
build/

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store

# Local design docs — not tracked
docs/

# Fastlane — secrets and build output (never commit the API key)
fastlane/.env
*.p8
*.ipa
fastlane/report.xml
fastlane/Preview.html
fastlane/test_output/
